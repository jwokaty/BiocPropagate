% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propagation.R
\name{check_propagation_from_results}
\alias{check_propagation_from_results}
\title{Check propagation status from pre-supplied build results}
\usage{
check_propagation_from_results(
  pkgName,
  pkg_data,
  branch = "release",
  criteria = default_results_criteria(),
  platforms = .DEFAULT_PLATFORMS,
  bioc_pkg_data = NULL,
  bioc_pkg_data_type = NULL,
  repo_path = NULL
)
}
\arguments{
\item{pkgName}{\code{character(1)} Package name.}

\item{pkg_data}{A named list shaped like the r-universe package
payload \code{\link[=check_propagation]{check_propagation()}} would otherwise fetch via
\code{.get_runi_meta()} -- at minimum \code{Package}, \code{Version},
\code{NeedsCompilation}, and \code{`_jobs`} (a data.frame with \code{config},
\code{r}, \code{check} columns). No \code{`_binaries`} is expected or used,
given \code{\link[=default_results_criteria]{default_results_criteria()}}'s platform checks never look for
it.}

\item{branch}{\code{character(1)} A Bioc status tag (\code{"release"}, \code{"devel"})
or explicit version (e.g. \code{"3.22"}).}

\item{criteria}{A criteria list, defaulting to
\code{\link[=default_results_criteria]{default_results_criteria()}}.}

\item{platforms}{\code{character()} OS-arch strings to check.}

\item{bioc_pkg_data}{\code{list(source, platform)}, or \code{NULL} (the default)
to fetch internally -- same as \code{\link[=check_propagation]{check_propagation()}}.}

\item{bioc_pkg_data_type}{\code{character(1)} or \code{NULL} (default) to
auto-detect via \code{.detect_bioc_pkg_type()}. Passed to
\code{.get_all_bioc_pkg_data()} when \code{bioc_pkg_data} isn't supplied.}

\item{repo_path}{\code{character(1)} or \code{NULL}. An already-cloned repo path
if the caller has one -- likely already true in a same-run build
context, since the workflow just checked the repo out to build it.}
}
\value{
\code{list(package, gates, platforms)} -- same shape as
\code{\link[=check_propagation]{check_propagation()}}.
}
\description{
For same-run use inside a build workflow -- e.g. a
GitHub Actions job running after a package's platform build matrix,
before deployment has happened and before the live r-universe API
reflects this run's results. Unlike \code{\link[=check_propagation]{check_propagation()}}, this does
not fetch \verb{_jobs}-shaped data itself; the caller supplies it
directly, already reconstructed from that run (e.g. via the GitHub
Actions jobs API and \code{"Conclude:"}-prefixed step names, the same
technique \code{r-universe-org/actions/deploy-packages}'s \code{jobsdata.sh}
uses).

Defaults to \code{\link[=default_results_criteria]{default_results_criteria()}} rather than
\code{\link[=default_criteria]{default_criteria()}} -- \verb{_binaries} genuinely doesn't exist yet at
this point in a build, so the \code{binary} platform check is excluded
rather than silently failing closed on data that was never going to
be there.
}
\examples{
pkg_data <- .example_pkg_data()
bioc_pkg_data <- list(
    source = data.frame(Package = "examplePkg", Version = "1.1.0")
)
check_propagation_from_results(
    "examplePkg", pkg_data, branch = "release", bioc_pkg_data = bioc_pkg_data
)

}
