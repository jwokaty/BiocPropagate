% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{register_criterion}
\alias{register_criterion}
\title{Register propagation criterion}
\usage{
register_criterion(criteria, name, fun, type = c("gates", "platform"))
}
\arguments{
\item{criteria}{A criteria list, e.g. from \code{\link[=default_criteria]{default_criteria()}}.}

\item{name}{\code{character(1)} Criterion name (replaces an existing one
with the same name and \code{type}).}

\item{fun}{A function returning \code{list(pass = logical(1), message = character(1))}. For \code{type = "gates"}:
\verb{function(pkg_data, branch, desc, views, repo)}. For
\code{type = "platform"}: \verb{function(pkg_data, branch, desc, views, platform)}.}

\item{type}{\code{character(1)} Either \code{"gates"} or \code{"platform"}.}
}
\value{
The updated criteria list.
}
\description{
Add or replace a named criterion in a criteria list,
without needing to know \code{\link[=default_criteria]{default_criteria()}}'s internal structure --
the hook point for propagation policy that changes over time.
}
\examples{
criteria <- default_criteria()
criteria <- register_criterion(
    criteria, "always_pass",
    function(pkg_data, branch, desc, views, repo)
        list(pass = TRUE, message = NA_character_),
    type = "gates"
)
names(criteria$gates)

}
