% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{default_criteria}
\alias{default_criteria}
\title{Default propagation criteria}
\usage{
default_criteria()
}
\value{
A list with two elements, \code{gates} and \code{platform}, each a named
list of criterion functions.
}
\description{
The default gate and platform criteria used by
\code{\link[=check_propagation]{check_propagation()}}. To opt \emph{out} of the git-based gates (e.g. to
avoid the clone cost), remove them explicitly:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{criteria <- default_criteria()
criteria$gates[names(git_criteria()$gates)] <- NULL
}\if{html}{\out{</div>}}

Copy and modify the returned list (via \code{\link[=register_criterion]{register_criterion()}}) to
add, remove, or override criteria as propagation policy evolves.
}
\examples{
criteria <- default_criteria()
names(criteria$gates)
names(criteria$platform)

}
