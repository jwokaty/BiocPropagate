% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{default_results_criteria}
\alias{default_results_criteria}
\title{Default criteria for check_propagation_from_results()}
\usage{
default_results_criteria()
}
\value{
A list with two elements, \code{gates} and \code{platform}, same shape
as \code{\link[=default_criteria]{default_criteria()}}.
}
\description{
Identical to \code{\link[=default_criteria]{default_criteria()}}, minus the \code{binary}
platform check. \verb{_binaries} doesn't exist yet at the point in a build
where pre-supplied results are available (binary uploads only happen
later, during deployment) -- see \code{\link[=check_propagation_from_results]{check_propagation_from_results()}}.
Including a criterion that can never see the data it needs would
mean every platform silently fails closed on that check alone,
rather than honestly reflecting that this is a narrower, same-run
check than \code{\link[=check_propagation]{check_propagation()}}'s full version.
}
\examples{
criteria <- default_results_criteria()
names(criteria$platform)
# "binary" is absent, unlike default_criteria()

}
